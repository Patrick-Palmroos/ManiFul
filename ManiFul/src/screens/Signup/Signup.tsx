import { View, Text } from 'react-native';
import colors from '../../styles/colors';

const Signup = () => {
  return (
    <View style={{ backgroundColor: colors.backgroundWarm, height: '100%' }}>
      <Text style={{ color: 'black' }}>Hi! SINGUP TIME!</Text>
    </View>
  );
};

export default Signup;
